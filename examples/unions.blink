option types_output = "./out/unions"

type values = u8 | string? | unknown

type union_of<A, B> = A | B
type u8_string = union_of<u8, string>

type foo = union "Tag" {
	First {},
	Second {}
}
type bar = union "Tag" {
	Third {},
	Fourth {}
}
type quax = struct {
	field: u8
}

type foo_bar_quax = u8 | foo | bar | quax

type model_instance = Instance<"Model"> | Instance
type button_enum = Enum<"Button"> | Enum

type state =
    "Idle"
    | "Running"
    | "Jumping"
    | "Falling"