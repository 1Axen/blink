scope first {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope second {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope third {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope fourth {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope fifth {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope sixth {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope first_a {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope second_a {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope third_a {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope fourth_a {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope fifth_a {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}

scope sixth_a {
    type a = u8[]
    type b = map {[string]: u8}

    type c = struct {
        field: u8
    }

	type d = 
    	"option"
		| "import"

    type d_tagged<T> = union "Type" {
        Some {
            Value: T
        },
        None {}
    }

    type e<K, V> = struct {
        key: K,
        value: V
    }

    -- type f = struct {
    --     some_field: u8
    -- } & c

    event g = {
        from: Server,
        type: Reliable,
        call: SingleSync,
        data: (a, b, c, d, d_tagged<unknown>, e<u8, u8>)
    }
}