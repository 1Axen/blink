option ClientOutput = "../../Network/ClientGenerics.luau"
option ServerOutput = "../../Network/ServerGenerics.luau"

struct Packet<T> {
    Sequence: u16,
    Ack: u16,
    AckBits: u32,
    Data: T,
}

struct Entity {
    id: u8,
    pos: vector,
    angle: u16,
}

event Snapshot {
    From: Client,
    Type: Unreliable,
    Call: SingleSync,
    Data: Packet<Entity>
}