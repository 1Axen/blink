option ClientOutput = "../Network/Client.luau"
option ServerOutput = "../Network/Server.luau"

type MainScopeType = u8
event MainScopeEvent = {
    From = Server,
    Type = Reliable,
    Call = SingleSync,
    Data = MainScopeType
}

scope ScopeExample = {
    type InScopeType = u8
    type SecondInScopeType = bool

    event InScopeEvent = {
        From = Server,
        Type = Reliable,
        Call = SingleSync,
        Data = MainScopeType
    }

    function InScopeFunction = {
        Yield = Coroutine,
        Data = u8,
        Return = u8
    }
}

struct TypeReferencingScopeType = {
    Field = ScopeExample.InScopeType
}